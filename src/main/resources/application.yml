micronaut:
  application:
    name: cassandraNative
cassandra:
  default:
    clusterName: '"cassandra"'
    contactPoint: '"localhost"'
    port: 9042
    maxSchemaAgreementWaitSeconds: 20
    ssl: true
